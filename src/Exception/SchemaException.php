<?php

/**
 * Class to provide a separate Exception so it can be caught separately.
 */

namespace PatternBuilder\Exception;

/**
 * Provide a separate Exception so it can be caught separately.
 */
class SchemaException extends Exception
{
}
